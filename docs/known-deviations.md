# Known deviations register

Wire-level and runtime behaviors of the current implementation that are believed to be
wrong, missing, or non-deterministic. Each entry is pinned by a characterization test
under `tests/characterization/` so that any change is deliberate. Entries marked
"strict xfail" also carry a test asserting the corrected behavior with
`pytest.mark.xfail(strict=True)`; when the fix lands that test starts passing and the
marker must be removed in the same commit.

"Corrected behavior" is stated only where public documentation is unambiguous. None of
the corrections below constitute a standards-compliance claim. Confidence levels:

- **High**: consistent across public references, real-vehicle captures, and open-source
  tools; only the exact clause text is unavailable.
- **Medium**: plausible from public references; needs the specification to settle.

| ID | Behavior today | Evidence | Corrected behavior | Confidence | Fix phase | Strict xfail | Status |
|---|---|---|---|---|---|---|---|
| DEV-01 | Physically addressed OBD requests on 0x7E0 are bound to a socket that is never read; only 0x7DF is served | `obd/listener.py:11-21` | Requests on the physical ID are answered on the response ID | High | 2 | yes | **Fixed** in Phase 2: the physical OBD endpoint is read; integration test `test_physical_request_is_answered` |
| DEV-02 | Mode 09 PID 02 response is `49 02 00` + VIN; item-count byte is 0x00 | `obd/responses.py:88` | Item count 0x01 | High | 5 | yes | **Fixed** in Phase 5: the byte is 0x01, one VIN per vehicle. The response length is unchanged at 20 bytes, so the multi-frame path and flow control are unaffected. Three independent real captures agree, including one worked example in the public ELM327 datasheet; see `docs/decisions/0003-phase-5-obd-evidence.md`. Not standards validated. Tests `test_mode09_pid02_vin_has_item_count_one`, `test_the_vin_item_count_is_one_and_the_length_is_unchanged`, integration `test_functional_vin_is_multi_frame` |
| DEV-03 | Mode 09 PID 0A response has no item-count byte and NUL-pads on the left | `obd/responses.py:98-102` | `49 0A 01` + 20 bytes; padding layout per specification | High for count byte; content Medium | 5 | yes, count byte and length only | Open |
| DEV-04 | Supported-PID bit 0 ("next range supported") is set for every range 0x00 to 0xC0 and for Mode 09 PID 00, whether or not PIDs exist in the next range | `obd/services.py:107-110` | Bit set only when at least one PID exists in the next range | High | 5 | yes | **Fixed** in Phase 5: the bit is derived from the parameter table, set only when the configured vehicle has an identifier beyond the range, and a range the chain never reaches is no longer answered either, so what is advertised and what is answered agree. Evidence in `docs/decisions/0003-phase-5-obd-evidence.md`; not standards validated. Tests `test_mode01_unadvertised_ranges_are_not_answered`, the DEV-04 section of `tests/unit/test_obd_pids.py`, integration `test_supported_pid_chain_terminates_on_the_wire` |
| DEV-05 | UDS 0x19 sub-function 0x02 requires a 2-byte request; the 3-byte form carrying `DTCStatusMask` gets NRC 0x13, and the mask is never applied | `uds/services.py:77` | `19 02 <mask>` is accepted and answered positively; mask filtering | High for framing; filtering Medium | 6 | yes, framing only | **Fixed** in Phase 6, as two deliberate wire changes: `19 02 <mask>` is answered positively, and the two-byte `19 02` that used to return every code is now `7F 19 13`, because the mask is a mandatory request parameter. A record is reported when `(status & mask) != 0`. Corroborated by the AUTOSAR Dcm specification, which states the filter rule verbatim, and by udsoncan and Scapy independently; see `docs/decisions/0004-phase-6-dtc-evidence.md`. Not standards validated. A third, smaller change went first in its own commit: the sub-function is now checked before the length it requires, so `19 82 FF` is `7F 19 12` rather than `7F 19 13`. Tests `test_0x19_02_with_a_status_mask_returns_the_matching_dtcs`, `test_0x19_02_without_a_status_mask_is_now_a_length_error`, `test_0x19_an_unknown_subfunction_is_rejected_as_a_subfunction_whatever_the_length`, integration `test_uds_read_dtc_by_status_mask_on_the_wire` |
| DEV-06 | Unsupported UDS SID on the physical address produces no response | `uds/services.py:48` | NRC 0x11 serviceNotSupported | High | 3 | yes | **Fixed** in Phase 3: each route carries an unsupported-service policy. Both physical routes answer a SID no protocol enabled there claims with `7F <SID> 11`; the OBD broadcast route stays silent, as a broadcast should. The legacy module is unchanged. Tests `test_unregistered_sid_on_a_physical_route_gets_nrc_0x11`, `test_unsupported_sids_on_the_physical_address_get_nrc_0x11`, integration `test_unknown_service_on_a_physical_address_gets_nrc_0x11` |
| DEV-07 | `suppressPosRspMsgIndicationBit` (0x80) in sub-functions of 0x10 and 0x11 is not masked; `10 83` returns NRC 0x12 | `uds/services.py:56,66` | Session change performed, no positive response sent | High | 7 (deferred from 3, then from 6) | yes | **Fixed** in Phase 7, and not where the entry implies. The bit is a framing rule about sub-functions, not a property of any one service, so it is handled once in `UdsProtocol.handle` for every service an explicit table declares to have a sub-function, and no handler contains a copy of it. Bit 7 is read, masked off before the service sees the sub-function, the service runs normally, and the response is withheld only if it turned out positive. `10 0x80|<supported session>` and `11 0x80|<supported reset>` are now silent where they were `7F 10 12` and `7F 11 12`. A negative response is never withheld, which is also the evidence that the masking happens: `11 85` is silent and `11 86` still answers `7F 11 12`, so the sub-function is being matched on its value rather than the answer dropped because bit 7 was set. Which services have a sub-function is configuration, never inferred from a payload shape -- 0x14's only served groupOfDTC is `FFFFFF`, whose second byte has bit 7 set, and masking it would refuse every clear. Corroborated by AUTOSAR CP R25-11 `[SWS_Dcm_00200]`, `[SWS_Dcm_00201]`, `[SWS_Dcm_00204]` and `[ECUC_Dcm_00737]`; ISO 14229-1:2026 clause 6.5 is unread. Not standards validated, and with **no interoperability evidence**: both client libraries set the bit and expect nothing back. Applying the rule honestly also reached 0x19, which is **DEV-24**. See `docs/decisions/0006-phase-7-scenario-and-testerpresent.md`. Tests `test_0x10_suppress_positive_response_bit_corrected`, `test_0x11_suppress_positive_response_bit_corrected`, `test_0x10_a_suppressed_request_for_an_unsupported_session_is_still_refused`, `test_a_service_without_a_subfunction_is_not_given_this_handling`, integration `test_a_suppressed_positive_response_is_not_transmitted`, `test_a_suppressed_request_does_not_hide_a_negative_response` |
| DEV-08 | ISO-TP TX padding is not enabled; short responses go out with DLC below 8 | `obd/listener.py:24-27` | 8-byte frames with configurable pad byte for OBD | High | 2 | no unit test possible; integration test in Phase 2 | **Fixed** in Phase 2 for OBD endpoints (pad byte 0x00, configurable); UDS unpadded until per-ECU config. Integration test `test_obd_response_frames_are_padded_to_dlc_8` |
| DEV-09 | Reading vehicle speed increments it; two consecutive `01 0D` reads differ | `obd/responses.py:37-46` | Reads have no side effects; value driven by scenario | n/a, design | 5 | yes | **Fixed** in Phase 5: Mode 01 parameters read `VehicleState` through signal paths and a read never mutates it. The fix-phase column read 7 until Phase 7 corrected it to 5, the phase the fix actually landed in; Phase 7 supplied the variation the corrected behavior anticipated, without reopening this entry. The value is static unless a profile configures a scenario, which the shipped profile does not. Tests `test_mode01_pid0d_speed_is_deterministic_and_has_no_side_effect`, `test_handling_a_request_does_not_mutate_the_vehicle` |
| DEV-10 | Coolant temperature is `random.randrange(130, 150)` | `obd/responses.py:49-50` | Deterministic value from state or scenario | n/a, design | 5 | yes | **Fixed** in Phase 5: the value comes from `engine.coolant_temp`, which the shipped profile sets to 90 degrees Celsius. No randomness remains in the OBD path, and none was introduced by the Phase 7 scenario engine, which has no randomness of any kind, seeded or otherwise. The fix-phase column read 7 until Phase 7 corrected it to 5. Test `test_mode01_pid05_coolant_is_deterministic` |
| DEV-11 | Modes 04 and 07 are accepted as valid SIDs but produce no response | `obd/services.py:23-37,69` | `44` for clear; `47` + pending DTCs | High for Mode 04; **Medium for Mode 07** | 6 (Mode 04); Mode 07 deferred | yes | **Mode 04 fixed** in Phase 6: `04` is answered `44` after clearing the shared store, and `04 00` is answered `44` without echoing the trailing byte. The ELM327 datasheet states the response byte verbatim and an independent tool-vendor reference agrees on the effects. **Mode 07 remains deferred and its strict xfail stays.** Its `47` + count framing rests on two open-source implementations and no public worked example, which is below the evidence bar in plan section 11; the generic `pending` state it would report exists in the store and is tested. What would unblock it: the J1979 text, or one or more independent real captures of a mode 07 CAN response. See `docs/decisions/0004-phase-6-dtc-evidence.md`. Tests `test_mode04_clear_dtcs_is_acknowledged`, `test_mode04_clears_the_shared_store`, `test_mode07_is_a_valid_sid_but_gets_no_response`, integration `test_clearing_over_obd_is_visible_to_uds_on_the_wire` |
| DEV-12 | Mode 01 PID 0C (RPM) and other common PIDs absent | `obd/services.py:25-30` | PID implemented | n/a, feature | 5 | yes for 0C | Open |
| DEV-13 | Malformed DTC strings in config are silently skipped by the encoder | `dtc_utils.py:17,25` | Rejected at configuration load | n/a, design | 4 | no | **Fixed** in Phase 4: the profile schema rejects any trouble code the encoder cannot encode, naming the offending value and its path, and also rejects duplicates and more than 255 of them. The accepted grammar is the one the encoder already accepts and the Phase 0 characterization tests pin; this is project input validation, not SAE J2012 validation. Tests `test_malformed_dtcs_are_rejected_at_load`, `test_every_dtc_the_encoder_accepts_is_accepted` |
| DEV-14 | Invalid config values: bad hex address calls `exit(1)`; out-of-range fuel level or type is silently replaced by a default; a negative fuel level passes validation and raises `OverflowError` at request time | `ecu_config.py:41-46`, `obd/responses.py:56-74` | Rejected at configuration load with a path-qualified error | n/a, design | 4 | no | **Fixed** in Phase 4: profiles are validated before anything runs and every problem is reported at once with its dotted path. A non-integer or out-of-range CAN identifier, a VIN over 17 characters, a fuel level outside 0 to 100 and a fuel type outside the coded range are all rejected; nothing calls `exit()`. Tests in `tests/unit/test_config_schema.py` and `test_a_malformed_address_is_rejected_at_load_instead_of_terminating_the_process` |
| DEV-15 | A Mode 03 request with a trailing byte (`03 00`) echoes that byte into the response: `43 00 02 ...` | `obd/services.py:48,54-59` | Undetermined; malformed request handling per specification | Medium | 5 | no | Open. Phase 6 added Mode 04 and deliberately did **not** give it the echo: `04 00` is answered `44`. Whoever resolves this decides for both, and for Mode 07 if it is ever implemented |
| DEV-16 | UDS DTC records use a fixed third byte 0x01 and fixed status 0x2F; 0x19 sub-functions 0x01 and 0x0A unsupported | `dtc_utils.py:9-11,22-28` | Status derived from DTC state | Medium | 6 (status), 11 (sub-functions); third byte **frozen** | no | **Status half fixed** in Phase 6: each record's status is derived from the shared store (bit 2 pending, bit 3 confirmed, bit 7 indicator requested) and the DTCStatusAvailabilityMask is the 0x8C those three bits make, not 0xFF. The bit layout is corroborated by the AUTOSAR Dem and by udsoncan and Scapy; which bits a simulator sets is a project decision. **Third byte frozen at 0x01**: it is the J2012 failure-type byte, the profile's five-character codes do not carry one, and no evidence supports any value including the current one. **0x19 sub-functions 0x01 and 0x0A remain Phase 11.** Tests `test_every_modelled_combination_of_flags_has_its_status_byte`, `test_no_producible_status_byte_sets_a_bit_outside_the_advertised_mask`, `test_the_frozen_third_byte_is_a_named_constant_so_the_freeze_is_visible` |
| DEV-17 | 0x10 session parameter record `00 1E 0B B8` (P2 = 30 ms, P2* = 30 s) and session changes have no effect | `uds/services.py:9,54-61` | Configurable, realistic defaults; session state | Medium | 11 | no | Open |
| DEV-18 | Mode 01 requests carrying several PIDs (`01 0D 0C`) answer only the first | `obd/listener.py:30-38` | Up to six PIDs per ISO 15765-4 request | High | 5, 5.1 | yes | **Fixed** in Phase 5.1: one response carries every requested parameter, service identifier once, each identifier echoed before its own data, in request order. At most six are answered, a parameter this vehicle cannot answer is omitted rather than refusing the request, and a repeated parameter is answered once, so no request answered before this change gets a different or shorter answer. The two worked CAN captures in the public ELM327 datasheet are reproduced byte for byte; see `docs/decisions/0005-phase-5-1-multi-pid-evidence.md`. Not standards validated. Tests `test_the_datasheet_capture_is_reproduced_byte_for_byte`, `test_the_datasheet_capture_in_a_different_order_answers_in_that_order`, `test_mode01_multi_parameter_request_answers_every_parameter`, integration `test_functional_multi_parameter_request`, `test_functional_six_parameter_response_is_multi_frame` |
| DEV-19 | CAN file logger uses python-can `socketcan_native`, removed in 4.x; thread crashes at start | `loggers/logger_can.py:7,22` | Logger removed in favour of `candump -l` | n/a | 1, 2 | no | **Fixed** in Phase 2: logger removed; use `candump -l` |
| DEV-20 | ISO-TP file logger blocks on four sockets in sequence and cannot log independently | `loggers/logger_isotp.py:19-23` | Removed | n/a | 2 | no | **Fixed** in Phase 2: logger removed |
| DEV-21 | Four non-daemon `while True` threads; SIGINT does not terminate the process | `ecu_simulator.py:37-50` | asyncio runtime with clean shutdown | n/a | 2 | no | **Fixed** in Phase 2: asyncio runtime, SIGINT/SIGTERM exit 0 (integration test `test_signal_shuts_down_cleanly`) |
| DEV-22 | Interface setup through `os.system` with string concatenation, `ifconfig`, `insmod` of an out-of-tree module | `ecu_simulator.py:24-34`, `setup_*.sh` | Standalone privileged scripts; in-tree `CAN_ISOTP` | n/a | 2 | no | **Fixed** in Phase 2: `scripts/setup_vcan.sh` / `setup_can.sh`, no `os.system`, in-tree `CAN_ISOTP` |
| DEV-23 | UDS 0x3E TesterPresent and 0x14 ClearDiagnosticInformation unsupported | `uds/services.py:34-38` | `7E 00` and `54` | High | 6 (0x14), 7 (0x3E) | yes | **0x14 fixed** in Phase 6: `14 FF FF FF` is answered `54` after `DtcStore.clear()`, the same clear OBD Mode 04 calls. Only groupOfDTC `FFFFFF` is served; any other group is `7F 14 31` and clears nothing, and any other length is `7F 14 13`, including the five-byte MemorySelection form. Corroborated by the AUTOSAR Dcm specification and two independent implementations; not standards validated. **0x3E fixed** in Phase 7: `3E 00` is answered `7E 00`, any other sub-function is `7F 3E 12`, and a request that is not two bytes is `7F 3E 13`, the sub-function being checked before the length it requires as Phase 6 chose for 0x19. Stateless by construction and tested as such: no timer is started, read or reset, no session state is created or consulted, and asking five times gives five identical answers and leaves the DTC store untouched. `3E 80` is silence, through the generic suppress-bit handling of DEV-07 rather than anything in the 0x3E handler. Corroborated by AUTOSAR `[SWS_Dcm_00251]` and, independently, by udsoncan and Scapy; ISO 14229-1:2026 is unread, so not standards validated. S3, session timing and Concurrent TesterPresent stay Phase 11. Before it changed, all six forms of the request were pinned at `7F 3E 11` in their own commit. Tests `test_0x3e_tester_present_corrected`, `test_0x3e_is_answered_by_the_uds_protocol`, `test_0x3e_answers_the_same_bytes_however_often_it_is_asked`, integration `test_tester_present_is_answered_on_the_wire`, `test_tester_present_changes_nothing_a_later_request_can_see`. Tests `test_0x14_clear_diagnostic_information_is_acknowledged`, `test_0x14_clears_what_mode_03_and_0x19_report`, `test_0x14_for_an_unsupported_group_is_out_of_range`, integration `test_clearing_over_uds_is_visible_to_obd_on_the_wire` |
| DEV-24 | `19 82 FF` returns `7F 19 12` and `19 82` returns `7F 19 12`: sub-function 0x82 is read as a report type this server does not support | `protocols/uds/protocol.py`, Phase 6 | `0x82` is report type `0x02` carrying the `suppressPosRspMsgIndicationBit` | High | 7 | no | **Fixed** in Phase 7, in its own commit, as a deliberate correction to behavior Phase 6 established. Found while implementing DEV-07: ReadDTCInformation has a sub-function -- the report type -- so the generic rule applies to it like any other sub-function service. `19 82 FF` now runs report type `0x02` and withholds its positive response, so nothing is transmitted; `19 82` now fails report type `0x02`'s own length rule and answers `7F 19 13`, which is the evidence that the bit is masked off and `0x02` then dispatched rather than the response being dropped. Unchanged: `19 02 FF` and every unsuppressed request byte for byte; the Phase 6 ordering rule, so an unsupported report type is still `0x12` at any length with or without bit 7; and the status mask, which is `0x02`'s own parameter and keeps all eight bits. The alternative considered and rejected was to declare 0x19 as having no sub-function, which would have preserved the Phase 6 bytes by recording something untrue in the table the whole rule reads from. Recorded as its own identifier rather than folded into DEV-07, whose text names only 0x10 and 0x11. `docs/decisions/0004-phase-6-dtc-evidence.md` still says what Phase 6 established at that checkpoint and is not rewritten. Not standards validated. Tests `test_0x19_82_is_report_type_0x02_with_its_positive_response_suppressed`, `test_0x19_82_without_a_status_mask_fails_report_type_0x02s_length_rule`, `test_0x19_a_report_type_this_server_lacks_is_still_refused_with_or_without_the_bit`, `test_the_status_mask_is_not_masked_on_its_way_to_the_service`, integration `test_reading_dtcs_can_be_asked_for_silently` |
| DEV-25 | A profile may give one ECU two physical endpoints on the same receive identifier with different transmit identifiers. The schema, `build_runtime` and `check_routes` all accept it, the kernel delivers every request to both sockets, and the simulator therefore **answers one request twice, on two identifiers** | `config/schema.py:233-236` requires only `(rx, tx)` pairs to be unique within an ECU; `schema.py:306-318` checks physical `rx` uniqueness only across ECUs. Measured 2026-09-26: `ice_default` plus an endpoint `rx 0x7E0 / tx 0x7EF` on the same ECU is accepted, and two ISO-TP sockets bound to `rx 0x7E0` both received a single-frame and a multi-frame request (vcan, kernel 6.8). Evidence recorded in `docs/decisions/0010-gui-observer-api.md` section 12, E5 and E6, on branch `gui` | Refuse at load a profile in which two receiving physical endpoints share a receive identifier, whichever ECU they belong to. Two functional endpoints on one identifier within one ECU **were not examined** and are not covered by this entry | High that the duplicate reply happens (measured); the corrected rule is a project decision | Unassigned: the owner schedules it. **Not part of any GUI milestone** | no. **Not yet pinned.** This register's rule requires a characterization test, and one must be added before any fix | **Open.** Not reachable with the shipped profiles (`ice_default`, `ice_scenario`). Recorded 2026-09-26. Does not change the Phase 8b gate or V1.0 acceptance |

Entries without a strict xfail are pinned by a plain golden test that asserts today's
bytes; changing them still requires editing that test.
